# Griddler2D

## Section

```elixir
alias Kino
```

```elixir
alias Griddlers2d.
Grid.make(2,2)
```

```elixir

```

```elixir
grid = Gc.build_grid(2,1)
```

```elixir
cells = Gc.cells(grid)
```

```elixir
lines = Gc.lines(grid)
```

```elixir
lines
|> Enum.map(&SVG.line_to_element/1)
|> SVG.to_svg()
|> Kino.Image.new(:svg)
```

```elixir
cells
|> Enum.map(&SVG.cell_to_element/1)
|> SVG.to_svg()
|> Kino.Image.new(:svg)
```
